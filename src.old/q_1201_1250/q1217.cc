#include <gtest/gtest.h>
#include <iostream>
#include <vector>

using namespace std;

/**
 * This file is generated by leetcode_add.py v1.0
 *
 * 1217.
 *       Minimum Cost to Move Chips to The Same Position
 *
 * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
 *
 *   We have ‘n’ chips, where the position of the ‘iᵗʰ’ chip is
 *   ‘position[i]’
 *   We need to move all the chips to “the same position” . In one step, we
 *   can change the position of the ‘iᵗʰ’ chip from ‘position[i]’
 *       -  ‘position[i] + 2’ or ‘position[i] - 2’ with ‘cost = 0’
 *       -  ‘position[i] + 1’ or ‘position[i] - 1’ with ‘cost = 1’
 *   Return “the minimum cost” needed to move all the chips to the same
 *   position.
 *
 * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
 *
 *   • ‘1 ≤ position.length ≤ 100’
 *   • ‘1 ≤ position[i] ≤ 10^9’
 *
 */

struct q1217 : public ::testing::Test {
  // Leetcode answer here
  class Solution {
   public:
    int minCostToMoveChips(vector<int>& position) {
      int even = 0, odd = 0;
      for (const auto& x : position) {
        if (x & 1) ++odd;
        else ++even;
      }
      return min(odd, even);
    }
  };

  class Solution *solution;
};

TEST_F(q1217, sample_input01) {
  solution = new Solution();
  vector<int> position = {1, 2, 3};
  int exp = 1;
  EXPECT_EQ(solution->minCostToMoveChips(position), exp);
  delete solution;
}

TEST_F(q1217, sample_input02) {
  solution = new Solution();
  vector<int> position = {2, 2, 2, 3, 3};
  int exp = 2;
  EXPECT_EQ(solution->minCostToMoveChips(position), exp);
  delete solution;
}

TEST_F(q1217, sample_input03) {
  solution = new Solution();
  vector<int> position = {1, 1000000000};
  int exp = 1;
  EXPECT_EQ(solution->minCostToMoveChips(position), exp);
  delete solution;
}