#include <gtest/gtest.h>
#include <iostream>
#include <string>

using namespace std;

/**
 * This file is generated by leetcode_add.py v1.0
 *
 * 1221.
 *       Split a String in Balanced Strings
 *
 * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
 *
 *    “Balanced” strings are those that have an equal quantity of ‘'L'’ and
 *   ‘'R'’
 *   Given a “balanced” string ‘s’ , split it in the maximum amount of
 *   balanced
 *   Return “the maximum amount of split “balanced” strings” .
 *
 * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
 *
 *   • ‘1 ≤ s.length ≤ 1000’
 *   • ‘s[i]’ is either ‘'L'’ or ‘'R'’ .
 *   • ‘s’ is a “balanced” string.
 *
 */

struct q1221 : public ::testing::Test {
  // Leetcode answer here
  class Solution {
   public:
    int balancedStringSplit(string s) {
      int res = 0, diff = 0;
      for (auto it = s.cbegin(); it != s.cend(); ++it) {
        diff += *it == 'L' ? 1 : -1;
        if (diff == 0) ++res;
      }
      return res;
    }
  };

  class Solution *solution;
};

TEST_F(q1221, sample_input01) {
  solution = new Solution();
  string s = "RLRRLLRLRL";
  int exp = 4;
  EXPECT_EQ(solution->balancedStringSplit(s), exp);
  delete solution;
}

TEST_F(q1221, sample_input02) {
  solution = new Solution();
  string s = "RLLLLRRRLR";
  int exp = 3;
  EXPECT_EQ(solution->balancedStringSplit(s), exp);
  delete solution;
}

TEST_F(q1221, sample_input03) {
  solution = new Solution();
  string s = "LLLLRRRR";
  int exp = 1;
  EXPECT_EQ(solution->balancedStringSplit(s), exp);
  delete solution;
}

TEST_F(q1221, sample_input04) {
  solution = new Solution();
  string s = "RLRRRLLRLL";
  int exp = 2;
  EXPECT_EQ(solution->balancedStringSplit(s), exp);
  delete solution;
}