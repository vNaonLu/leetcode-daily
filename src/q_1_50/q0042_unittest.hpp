
#ifndef Q42_UNITTEST_H__
#define Q42_UNITTEST_H__
#include <gtest/gtest.h>

#include "q0042.hpp"
using namespace std;

/**
 * This file is generated by leetcode_add.py
 *
 * 42.
 *     Trapping Rain Water
 *
 * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
 *
 *   Given ‘n’ non-negative integers representing an elevation map where
 *   the width of each bar is ‘1’ , compute how much water it can trap
 *   after raining.
 *
 * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
 *
 *   • ‘n = height.length’
 *   • ‘1 ≤ n ≤ 2 × 10⁴’
 *   • ‘0 ≤ height[i] ≤ 10⁵’
 *
 */

TEST(q42, sample_input01) {
  l42::Solution solver;
  vector<int> height = {0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1};
  int exp = 6;
  EXPECT_EQ(solver.trap(height), exp);
}

TEST(q42, sample_input02) {
  l42::Solution solver;
  vector<int> height = {4, 2, 0, 3, 2, 5};
  int exp = 9;
  EXPECT_EQ(solver.trap(height), exp);
}

#endif