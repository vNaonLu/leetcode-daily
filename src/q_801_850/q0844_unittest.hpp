
#ifndef Q844_UNITTEST_H__
#define Q844_UNITTEST_H__
#include <gtest/gtest.h>

#include "q0844.hpp"
using namespace std;

/**
  * this file is generated by gquestion.py
  *
  * 844. Backspace String Compare
  *
  * Given two strings s and t , return true if they
  * are equal when both are typed into empty text
  * editors . ';#'; means a backspace character.
  * Note that after backspacing an empty text, the
  * text will continue empty.
  *
  * Constraints:
  * -  <span>1 <= s.length, t.length <= 200</span> 
  * - <span> s and t only containlowercase letters and ';#'; characters.</span>
  *  “Follow up:“ Can you solve it in O(n) time and O(1) space?
  *
*/

TEST(q844, sample_input01) {
  string s = "ab#c",
         t = "ad#c";
  bool exp = true;
  l844::Solution solver;
  EXPECT_EQ(solver.backspaceCompare(s, t), exp);
}

TEST(q844, sample_input02) {
  string s = "ab##",
         t = "c#d#";
  bool exp = true;
  l844::Solution solver;
  EXPECT_EQ(solver.backspaceCompare(s, t), exp);
}

TEST(q844, sample_input03) {
  string s = "a##c",
         t = "#a#c";
  bool exp = true;
  l844::Solution solver;
  EXPECT_EQ(solver.backspaceCompare(s, t), exp);
}

TEST(q844, sample_input04) {
  string s = "a#c",
         t = "b";
  bool exp = false;
  l844::Solution solver;
  EXPECT_EQ(solver.backspaceCompare(s, t), exp);
}

#endif