
#ifndef Q413_UNITTEST_H__
#define Q413_UNITTEST_H__
#include <gtest/gtest.h>

#include "q0413.hpp"
using namespace std;

/**
  * This file is generated by leetcode_add.py
  *
  * 413.
  *      Arithmetic Slices
  *
  * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
  *
  *   An integer array is called arithmetic if it consists of “at least
  *   three elements” and if the difference between any two consecutive
  *   elements is the same.
  *   
  *   - For example, ‘[1,3,5,7,9]’ , ‘[7,7,7,7]’ , and ‘[3,-1,-5,-9]’ are
  *   arithmetic sequences.
  *   
  *   Given an integer array ‘nums’ , return “the number of arithmetic
  *   “subarrays” of” ‘nums’.
  *   
  *   A “subarray” is a contiguous subsequence of the array.
  *
  * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
  *
  *   • ‘1 ≤ nums.length ≤ 5000’
  *   • ‘-1000 ≤ nums[i] ≤ 1000’
  *
*/

TEST(q413, sample_input01) {
  l413::Solution solver;
  vector<int> nums = {1, 2, 3, 4};
  int exp = 3;
  EXPECT_EQ(solver.numberOfArithmeticSlices(nums), exp);
}

TEST(q413, sample_input02) {
  l413::Solution solver;
  vector<int> nums = {1};
  int exp = 0;
  EXPECT_EQ(solver.numberOfArithmeticSlices(nums), exp);
}

#endif