
#ifndef Q415_UNITTEST_H__
#define Q415_UNITTEST_H__
#include <gtest/gtest.h>

#include "q0415.hpp"
using namespace std;

/**
  * this file is generated by gquestion.py
  *
  * 415. Add Strings
  * Given two non-negative integers, num1 and num2
  * represented as string, return the sum of num1 and
  * num2 as a string .
  * You must solve the problem without using any
  * built-in library for handling large integers (such as
  * BigInteger ). You must also not convert the inputs
  * to integers directly.
  *
  * Constraints:
  * -  1 <= num1.length, num2.length <= 10^(4) 
  * -  num1 and num2 consist of only digits.
  * -  num1 and num2 don';t have any leading zeros except for the zero itself.
  *
*/

TEST(q415, sample_input01) {
  string num1 = "11",
         num2 = "123";
  string exp = "134";
  l415::Solution solver;
  EXPECT_EQ(solver.addStrings(num1, num2), exp);
}

TEST(q415, sample_input02) {
  string num1 = "456",
         num2 = "77";
  string exp = "533";
  l415::Solution solver;
  EXPECT_EQ(solver.addStrings(num1, num2), exp);
}

TEST(q415, sample_input03) {
  string num1 = "0",
         num2 = "0";
  string exp = "0";
  l415::Solution solver;
  EXPECT_EQ(solver.addStrings(num1, num2), exp);
}

#endif