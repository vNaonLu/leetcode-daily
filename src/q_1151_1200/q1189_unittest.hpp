
#ifndef Q1189_UNITTEST_H__
#define Q1189_UNITTEST_H__
#include <gtest/gtest.h>

#include "q1189.hpp"
using namespace std;

/**
  * this file is generated by gquestion.py
  *
  * 1189. Maximum Number of Balloons
  *
  * Given a string text , you want to use the
  * characters of text to form as many instances of the word
  * “"balloon"“ as possible.
  * You can use each character in text “at most once“
  * . Return the maximum number of instances that can
  * be formed.
  *
  * Constraints:
  * -  1 <= text.length <= 10^(4) 
  * -  text consists of lower case English letters only.
  *
*/

TEST(q1189, sample_input01) {
  string text = "nlaebolko";
  int exp = 1;
  l1189::Solution solver;
  EXPECT_EQ(solver.maxNumberOfBalloons(text), exp);
}

TEST(q1189, sample_input02) {
  string text = "loonbalxballpoon";
  int exp = 2;
  l1189::Solution solver;
  EXPECT_EQ(solver.maxNumberOfBalloons(text), exp);
}

TEST(q1189, sample_input03) {
  string text = "leetcode";
  int exp = 0;
  l1189::Solution solver;
  EXPECT_EQ(solver.maxNumberOfBalloons(text), exp);
}

#endif