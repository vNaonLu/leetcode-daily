
#ifndef Q215_UNITTEST_H__
#define Q215_UNITTEST_H__
#include <gtest/gtest.h>

#include "q0215.hpp"
using namespace std;

/**
  * This file is generated by leetcode_add.py
  *
  * 215.
  *      Kth Largest Element in an Array
  *
  * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
  *
  *   Given an integer array ‘nums’ and an integer ‘k’ , return “the” ‘kᵗʰ’
  *   “largest element in the array”.
  *   
  *   Note that it is the ‘kᵗʰ’ largest element in the sorted order, not the
  *   ‘kᵗʰ’ distinct element.
  *
  * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
  *
  *   • ‘1 ≤ k ≤ nums.length ≤ 10⁴’
  *   • ‘-10⁴ ≤ nums[i] ≤ 10⁴’
  *
*/

TEST(q215, sample_input01) {
  l215::Solution solver;
  vector<int> nums = {3, 2, 1, 5, 6, 4};
  int k = 2;
  int exp = 5;
  EXPECT_EQ(solver.findKthLargest(nums, k), exp);
}

TEST(q215, sample_input02) {
  l215::Solution solver;
  vector<int> nums = {3, 2, 3, 1, 2, 4, 5, 5, 6};
  int k = 4;
  int exp = 4;
  EXPECT_EQ(solver.findKthLargest(nums, k), exp);
}

#endif