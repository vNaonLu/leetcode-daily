
#ifndef Q1044_UNITTEST_H__
#define Q1044_UNITTEST_H__
#include <gtest/gtest.h>

#include "q1044.hpp"
using namespace std;

/**
 * This file is generated by leetcode_add.py
 *
 * 1044.
 *       Longest Duplicate Substring
 *
 * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
 *
 *   Given a string ‘s’ , consider all “duplicated substrings” :
 *   (contiguous) substrings of s that occur 2 or more times.The
 *   occurrencesmay
 *   Return “any” duplicatedsubstring that has the longest possible
 *   length.If ‘s’ does not have a duplicated substring, the answer is ‘''’
 *   .
 *
 * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
 *
 *   • ‘2 ≤ s.length ≤ 3 × 10⁴’
 *   • ‘s’ consists of lowercase English letters.
 *
 */

TEST(q1044, sample_input01) {
  l1044::Solution solver;
  string s = "banana";
  string exp = "ana";
  EXPECT_EQ(solver.longestDupSubstring(s), exp);
}

#endif