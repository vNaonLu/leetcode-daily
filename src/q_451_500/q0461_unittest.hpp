
#ifndef Q461_UNITTEST_H__
#define Q461_UNITTEST_H__
#include <gtest/gtest.h>

#include "q0461.hpp"
using namespace std;

/**
 * This file is generated by leetcode_add.py
 *
 * 461.
 *      Hamming Distance
 *
 * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
 *
 *   The <a href="https://en.wikipedia.org/wiki/Hamming_distance"
 *   target="_blank">Hamming distance</a> between two integers is the
 *   number of positions at which the corresponding bits are
 *   Given two integers ‘x’ and ‘y’ , return “the “Hamming distance”
 *   between them” .
 *
 * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
 *
 *   • ‘0 ≤x, y ≤ 2³¹ - 1’
 *
 */

TEST(q461, sample_input01) {
  l461::Solution solver;
  int x = 1;
  int y = 4;
  int exp = 2;
  EXPECT_EQ(solver.hammingDistance(x, y), exp);
}

TEST(q461, sample_input02) {
  l461::Solution solver;
  int x = 3;
  int y = 1;
  int exp = 1;
  EXPECT_EQ(solver.hammingDistance(x, y), exp);
}

#endif