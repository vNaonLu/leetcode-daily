#include <gtest/gtest.h>
#include <iostream>
#include <string>

using namespace std;

/**
 * This file is generated by leetcode_add.py
 *
 * 389.
 *      Find the Difference
 *
 * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
 *
 *   You are given two strings ‘s’ and ‘t’
 *   String ‘t’ is generated by random shuffling string ‘s’ and then add
 *   one more letter at a random
 *   Return the letter that was added to ‘t’ .
 *
 * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
 *
 *   • ‘0 ≤ s.length ≤ 1000’
 *   • ‘t.length = s.length + 1’
 *   • ‘s’ and ‘t’ consist of lowercase English letters.
 *
 */

struct q389 : public ::testing::Test {
  // Leetcode answer here
  class Solution {
   public:
    char findTheDifference(string s, string t) {
      vector<int> cnt(26, 0);
      for (const auto &c : s) ++cnt[c - 'a'];
      for (const auto &c : t) --cnt[c - 'a'];
      for (int i = 0; i < cnt.size(); ++i) {
        if (cnt[i] != 0) return 'a' + i;
      }
      return ' ';
    }
  };

  class Solution *solution;
};

TEST_F(q389, sample_input01) {
  solution = new Solution();
  string s = "abcd";
  string t = "abcde";
  char exp = 'e';
  char act = solution->findTheDifference(s, t);
  EXPECT_EQ(act, exp);
  delete solution;
}

TEST_F(q389, sample_input02) {
  solution = new Solution();
  string s = "";
  string t = "y";
  char exp = 'y';
  char act = solution->findTheDifference(s, t);
  EXPECT_EQ(act, exp);
  delete solution;
}