#include <gtest/gtest.h>
#include <iostream>
#include <string>

using namespace std;

/**
 * This file is generated by leetcode_add.py
 *
 * 709.
 *      To Lower Case
 *
 * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
 *
 *   Given a string ‘s’ , return “the string after replacing every
 *   uppercase letter with the same lowercase letter” .
 *
 * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
 *
 *   • ‘1 ≤ s.length ≤ 100’
 *   • ‘s’ consists of printable ASCII characters.
 *
 */

struct q709 : public ::testing::Test {
  // Leetcode answer here
  class Solution {
   public:
    string toLowerCase(string s) {
      string res = s;
      for (auto &c : res) {
        if (c >= 'A' && c <= 'Z') {
          c = 'a' + c - 'A';
        }
      }

      return res;
    }
  };

  class Solution *solution;
};

TEST_F(q709, sample_input01) {
  solution = new Solution();
  string s = "Hello";
  string exp = "hello";
  string act = solution->toLowerCase(s);
  EXPECT_EQ(act, exp);
  delete solution;
}

TEST_F(q709, sample_input02) {
  solution = new Solution();
  string s = "here";
  string exp = "here";
  string act = solution->toLowerCase(s);
  EXPECT_EQ(act, exp);
  delete solution;
}

TEST_F(q709, sample_input03) {
  solution = new Solution();
  string s = "LOVELY";
  string exp = "lovely";
  string act = solution->toLowerCase(s);
  EXPECT_EQ(act, exp);
  delete solution;
}