
#ifndef Q1137_UNITTEST_H__
#define Q1137_UNITTEST_H__
#include <gtest/gtest.h>

#include "q1137.hpp"
using namespace std;

/**
  * this file is generated by gquestion.py
  *
  * 1137. N-th Tribonacci Number
  * The Tribonacci sequence T⎽(n) is defined as
  * follows:
  * T⎽(0) = 0, T⎽(1) = 1, T⎽(2) = 1, and T⎽(n+3) =
  * T⎽(n) + T⎽(n+1) + T⎽(n+2) for n >= 0.
  * Given n , return the value of T⎽(n).
  *
  * Constraints:
  * -  0 <= n <= 37 
  * - The answer is guaranteed to fit within a 32-bit integer, ie. answer <= 2^31 - 1 .
  *
*/

TEST(q1137, sample_input01) {
  int n = 4;
  int exp = 4;
  l1137::Solution solver;
  EXPECT_EQ(solver.tribonacci(n), exp);
}

TEST(q1137, sample_input02) {
  int n = 25;
  int exp = 1389537;
  l1137::Solution solver;
  EXPECT_EQ(solver.tribonacci(n), exp);
}

#endif