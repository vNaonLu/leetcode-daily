#include <gtest/gtest.h>
#include <iostream>

using namespace std;

/**
 * This file is generated by leetcode_add.py
 *
 * 559.
 *      Maximum Depth of N-ary Tree
 *
 * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
 *
 *   Given a n-ary tree, find its maximum
 *   The maximum depth is the number of nodes along the longest path from
 *   the root node down to the farthest leaf
 *    “Nary-Tree input serialization is represented in their level order
 *   traversal, each group of children is separated by the null value (See
 *   examples).”
 *
 * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
 *
 *   • The total number of nodes is in the range ‘[0, 10⁴]’ .
 *   • The depth of the n-ary tree is less than or equal to ‘1000’ .
 *
 */

struct q559 : public ::testing::Test {
  // Leetcode answer here
  class Node {
  public:
    int            val;
    vector<Node *> children;

    Node() {}

    Node(int _val) { val = _val; }

    Node(int _val, vector<Node *> _children) {
      val      = _val;
      children = _children;
    }
  };

  class Solution {
  public:
    int maxDepth(Node *root) {
      if (nullptr == root) {
        return 0;
      }
      auto depth = (int)0;
      for (auto p : root->children) {
        depth = max(depth, maxDepth(p));
      }
      return 1 + depth;
    }
  };

  class Solution *solution;
};

TEST_F(q559, NOT_IMPLEMENT) { EXPECT_TRUE("NOT IMPLEMENT"); }