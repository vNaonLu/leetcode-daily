
#ifndef Q509_UNITTEST_H__
#define Q509_UNITTEST_H__
#include <gtest/gtest.h>

#include "q0509.hpp"
using namespace std;

/**
  * This file is generated by leetcode_add.py
  *
  * 509.
  *      Fibonacci Number
  *
  * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
  *
  *   The “Fibonacci numbers” , commonly denoted ‘F(n)’ form a sequence,
  *   called the “Fibonacci sequence” , such that each number is the sum of
  *   the two preceding ones, starting from ‘0’ and ‘1’ . That.
  *   
  *   F(0) = 0, F(1) =.
  *   
  *   F(n) = F(n - 1) + F(n - 2), for n > 1.
  *   
  *   Given ‘n’ , calculate ‘F(n)’.
  *
  * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
  *
  *   • ‘0 ≤ n ≤ 30’
  *
*/

TEST(q509, sample_input01) {
  l509::Solution solver;
  int n = 2;
  int exp = 1;
  EXPECT_EQ(solver.fib(n), exp);
}

TEST(q509, sample_input02) {
  l509::Solution solver;
  int n = 3;
  int exp = 2;
  EXPECT_EQ(solver.fib(n), exp);
}

TEST(q509, sample_input03) {
  l509::Solution solver;
  int n = 4;
  int exp = 3;
  EXPECT_EQ(solver.fib(n), exp);
}

#endif