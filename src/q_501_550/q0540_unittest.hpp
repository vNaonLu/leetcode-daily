
#ifndef Q540_UNITTEST_H__
#define Q540_UNITTEST_H__
#include <gtest/gtest.h>

#include "q0540.hpp"
using namespace std;

/**
  * This file is generated by leetcode_add.py
  *
  * 540.
  *      Single Element in a Sorted Array
  *
  * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
  *
  *   You are given a sorted array consisting of only integers where every
  *   element appears exactly twice, except for one element which appears
  *   exactly once.
  *   
  *   Return “the single element that appears only once”.
  *   
  *   Your solution must run in ‘O(log n)’ time and ‘O(1)’ space.
  *
  * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
  *
  *   • ‘1 ≤ nums.length ≤ 10⁵’
  *   • ‘0 ≤ nums[i] ≤ 10⁵’
  *
*/

TEST(q540, sample_input01) {
  l540::Solution solver;
  vector<int> nums = {1, 1, 2, 3, 3, 4, 4, 8, 8};
  int exp = 2;
  EXPECT_EQ(solver.singleNonDuplicate(nums), exp);
}

TEST(q540, sample_input02) {
  l540::Solution solver;
  vector<int> nums = {3, 3, 7, 7, 10, 11, 11};
  int exp = 10;
  EXPECT_EQ(solver.singleNonDuplicate(nums), exp);
}

#endif