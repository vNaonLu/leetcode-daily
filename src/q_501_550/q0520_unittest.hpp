
#ifndef Q520_UNITTEST_H__
#define Q520_UNITTEST_H__
#include <gtest/gtest.h>

#include "q0520.hpp"
using namespace std;

/**
 * This file is generated by leetcode_add.py
 *
 * 520.
 *      Detect Capital
 *
 * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
 *
 *   We define the usage of capitals in a word to be right when one of the
 *   following cases
 *       - All letters in this word are capitals, like ‘'USA'’
 *       - All letters in this word are not capitals, like ‘'leetcode'’
 *       - Only the first letter in this word is capital, like ‘'Google'’
 *   Given a string ‘word’ , return ‘true’ if the usage of capitals in it
 *   is right.
 *
 * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
 *
 *   • ‘1 ≤ word.length ≤ 100’
 *   • ‘word’ consists of lowercase and uppercase English letters.
 *
 */

TEST(q520, sample_input01) {
  l520::Solution solver;
  string word = "USA";
  bool exp = true;
  EXPECT_EQ(solver.detectCapitalUse(word), exp);
}

TEST(q520, sample_input02) {
  l520::Solution solver;
  string word = "FlaG";
  bool exp = false;
  EXPECT_EQ(solver.detectCapitalUse(word), exp);
}

#endif