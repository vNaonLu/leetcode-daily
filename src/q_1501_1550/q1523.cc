#include <gtest/gtest.h>
#include <iostream>

using namespace std;

/**
 * This file is generated by leetcode_add.py
 *
 * 1523.
 *       Count Odd Numbers in an Interval Range
 *
 * ––––––––––––––––––––––––––––– Description –––––––––––––––––––––––––––––
 *
 *   Given two non-negative integers ‘low’ and ‘<font
 *   face="monospace">high</font>’ . Return the “count of odd numbers
 *   between ” ‘low’ “ and ” ‘<font face="monospace">high</font>’
 *   “(inclusive)”
 *   
 *
 * ––––––––––––––––––––––––––––– Constraints –––––––––––––––––––––––––––––
 *
 *   • ‘0 ≤ low ≤ high≤ 10^9’
 *
 */

struct q1523 : public ::testing::Test {
  // Leetcode answer here
  class Solution {
   public:
    int countOdds(int low, int high) {
      return (high - low) / 2 + (high & 1 || low & 1);
    }
  };

  class Solution *solution;
};

TEST_F(q1523, sample_input01) {
  solution = new Solution();
  int low = 3;
  int high = 7;
  int exp = 3;
  int act = solution->countOdds(low, high);
  EXPECT_EQ(act, exp);
  delete solution;
}

TEST_F(q1523, sample_input02) {
  solution = new Solution();
  int low = 8;
  int high = 10;
  int exp = 1;
  int act = solution->countOdds(low, high);
  EXPECT_EQ(act, exp);
  delete solution;
}